{
  "name": "flexable-deploy",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "SLS_DEBUG=* serverless deploy",
    "deploy:post": "SLS_DEBUG=* serverless deploy"
  },
  "author": "serverless.com",
  "license": "MIT",
  "devDependencies": {
    "serverless-google-cloudfunctions": "^1.2.0"
  },
  "dependencies": {
    "faker": "^4.1.0"
  }
}
