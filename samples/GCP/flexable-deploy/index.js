const { createPost, updatePost } = require('./src/modules/post');
const { createUser, updateUser } = require('./src/modules/user');

module.exports = {
  createPost,
  updatePost,
  createUser,
  updateUser
};
